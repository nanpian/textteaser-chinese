TextTeaser-Chinese文本摘要中文版
=============

### How to Use

    >>> from textteaser import TextTeaser
    >>> tt = TextTeaser()
    >>> tt.summarize(title, text)

You can also test TextTeaser by running `python test.py`.
# textteaser-chinese
